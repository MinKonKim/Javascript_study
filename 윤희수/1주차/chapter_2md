##1-4-1 불변값(기본형 데이터)
  변수 - 바꿀 수 있는 값
  상수 - 바꿀 수 없는 값
 한 번 만든 값을 바꿀 수 없고, 숫자 값도 다른 값으로 변경할 수 없다.
변경은 새로 만드는 동작을 통해서만 이뤄진다.

##1-4-2 가변값(참조형 데이터)
  객체의 변수 (프로퍼티)
  데이터 영역에 저장된 값은 모두 불변값이지만, 변수에는 다른 값을 얼마든지 대입할 수 있다.
  이것을 참조형 데이터는 불변하지 않는다(가변값이다)고 한다.
